[project]
name = "fetch-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "northernmarshes", email = "northernmarshes@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
    "requests>=2.34.2",
]

[project.scripts]
fetch-data = "fetch_data:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
